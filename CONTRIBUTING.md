# 贡献指南

感谢你关注 FinSightPro！欢迎以 Issue、讨论或 Pull Request 的形式参与贡献。

## 行为准则

参与本项目即表示你同意遵守 [行为准则](CODE_OF_CONDUCT.md)。请对所有贡献者与使用者保持友善与尊重。

## 如何贡献

### 报告 Bug

提交 Issue 时请尽量包含：

1. 操作系统与 Python 版本；
2. 复现步骤（越具体越好）；
3. 期望行为与实际行为；
4. 关键报错日志（**请务必脱敏，不要粘贴你的 API Key 或任何私密配置**）。

### 提交功能建议

欢迎先开 Issue 讨论再动手实现，避免重复劳动。与「多空对抗辩论引擎」「数据源扩展」「前端可视化」相关的建议尤其受欢迎。

### 提交 Pull Request

1. Fork 仓库并从 `main` 创建功能分支；
2. 确保代码可通过全部测试：

   ```bash
   pip install -r requirements.txt
   pip install pytest
   pytest tests/ -v
   ```

3. 新功能请附带对应测试（本项目测试全部离线运行，使用 mock，不得依赖真实 LLM API 或真实行情网络）；
4. 提交信息使用清晰的中英文皆可，推荐格式：`类型: 简述`（如 `feat: 支持港股代码解析`、`fix: 修复缓存过期判断`）；
5. 保持既有代码风格（中文注释、模块级 docstring、类型注解）。

## 开发注意事项

- **绝对不要**把 `.env`、API Key 或任何密钥提交进仓库（`.gitignore` 已排除，仍请自查）；
- 不要提交 `instance/`、`result/`、`cache/` 下的运行时数据；
- 涉及金融数据的代码须遵守项目合规边界：仅使用公开数据、输出仅供研究参考、不构成投资建议；
- 新增依赖请评估必要性，优先使用标准库与既有依赖。

## 许可

提交即表示你同意代码以 [MIT License](LICENSE) 随本项目发布。
