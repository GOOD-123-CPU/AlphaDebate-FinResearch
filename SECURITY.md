# 安全策略

## 支持的版本

| 版本 | 支持情况 |
|---|---|
| main 分支最新提交 | ✅ 支持 |
| 其他历史提交 | ❌ 请升级到最新版本 |

## 报告安全漏洞

如果你发现了安全漏洞，**请勿在公开 Issue 中披露细节**。

请通过以下方式私下报告：

1. 使用 GitHub 的「Report a vulnerability」（私有漏洞报告）功能（仓库 Security 标签页）；或
2. 通过仓库主页提供的维护者联系方式私下联系。

报告时请尽量包含：漏洞描述、复现步骤、影响范围、可能的修复建议。我们会在收到报告后尽快确认并跟进修复，修复发布后会公开致谢报告者（除非你希望匿名）。

## 安全设计说明

本项目在设计中已考虑以下安全边界，供审计参考：

- **密钥管理**：所有 API Key 通过 `.env` 环境变量注入，源码零硬编码密钥；`.env` 已被 `.gitignore` 排除；
- **路径安全**：报告读取接口对文件名做了目录穿越（`../`、反斜杠）防护；
- **XSS 防护**：前端所有 Markdown 渲染统一经 DOMPurify 消毒；
- **登录态校验**：报告生成/读取/元数据/搜索等接口均要求会话登录；
- **SQL 注入**：数据访问统一经 SQLAlchemy ORM 参数化查询。

## 已知边界（非漏洞，使用须知）

- 本项目定位为**本地/内网部署的研究工具**。若部署到公网，请自行追加 HTTPS、反向代理鉴权、速率限制与账号审计等生产级防护；
- Flask 开发服务器（`python run.py`）仅供开发调试，生产部署请使用 gunicorn/waitress（见 `docs/deployment.md`）；
- 本项目不提供任何投资建议，生成的研报内容由 AI 生成，可能存在错误，请自行核实。
